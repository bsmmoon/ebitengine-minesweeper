version: "3.8"
services:
  dev:
    build: .
    image: go-dev-124:latest
    container_name: go-dev
    user: dev
    working_dir: /workspace
    volumes:
      - .:/workspace
    command: sleep infinity
